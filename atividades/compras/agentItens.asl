
/*Opções de itens*/
item(fini).
item(sorvete).
item(chocolate).

/*Opcao local p/ a compra*/
local(fisica).
local(online).

/*Escolher quais opções*/
compra(add).
compra(addP).
compra(comp).
compra(compP).
compra(addComp).
compra(addCompP).

/*Excluir item*/
remover(sim).
remover(nao).
