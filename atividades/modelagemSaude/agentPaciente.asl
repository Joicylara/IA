// Agent agentPaciente in project modelagemSaude.mas2j

saude(boa).
saude(doente).

sintoma(garganta).
sintoma(tosse).
sintoma(febre).

doenca(gripe).
doenca(amigdalite).
doenca(bronquite).

tratamentoNecessario(sim).
tratamentoNecessario(nao).

cor(verde).
cor(amarelo).
cor(vermelho).
