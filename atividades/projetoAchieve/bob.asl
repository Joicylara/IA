// Agent bob in project projetoAchieve.mas2j

/* Initial beliefs and rules */

/* Initial goals */


/*+!ligar(ok)<-
	.print("Luz ligada!");
	.send(kate, tell, luz(ligada));
	!ligar(ok).*/
	nome(bob).
	nome(alice).
